import React from 'react';

//props.images destructured
const ImageList = ({images}) => {
    console.log(images);
    return (
        <div>
          ImageList  
        </div>
    );
};

export default ImageList;